# ============================================================================
# Application Configuration
# ============================================================================

APP_NAME="Kanban Board"

# Options: local, testing, staging, production
APP_ENV=local

# Application Key (generate with: php artisan key:generate)
APP_KEY=base64:pfEC2AyD/SJrTnMecMUiUdlRqHCMr9dnQCAto4bcNIE=

# Application Debug Mode
# - true: только для local и testing (показывает детальные ошибки)
# - false: обязательно для staging и production (безопасность)
APP_DEBUG=true

APP_URL=http://localhost:8081

APP_LOCALE=ru
APP_FALLBACK_LOCALE=ru
APP_FAKER_LOCALE=ru_RU

# - local/testing: stack (собирает логи в файл)
# - staging/production: stdout (JSON-логи для Docker/Loki)
LOG_CHANNEL=stack

# Options: single, daily, slack, syslog
LOG_STACK=single

# Log Deprecations Channel
LOG_DEPRECATIONS_CHANNEL=null

# - local/testing: debug (все логи)
# - staging: info (важные события)
# - production: warning (только предупреждения и ошибки)
LOG_LEVEL=debug

DB_CONNECTION=pgsql

DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=kanban
DB_USERNAME=kanban
DB_PASSWORD=kanban_password

SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

CACHE_STORE=redis

QUEUE_CONNECTION=database

RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=kanban
RABBITMQ_PASS=kanban_password
RABBITMQ_VHOST=/
RABBITMQ_QUEUE=default
RABBITMQ_URL=amqp://kanban:kanban_password@rabbitmq:5672/

FILESYSTEM_DISK=local

BROADCAST_DRIVER=redis

REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

WEBSOCKETS_HOST=websockets
WEBSOCKETS_PORT=6001

PUSHER_APP_ID=kanban
PUSHER_APP_KEY=kanban-key
PUSHER_APP_SECRET=kanban-secret
PUSHER_APP_CLUSTER=mt1

SPEECH_SERVICE_URL=http://speech-service:8083

AI_RABBITMQ_TIMEOUT=3.0
AI_RABBITMQ_RW_TIMEOUT=3.0
AI_RABBITMQ_HEARTBEAT=30

MAIL_MAILER=log

MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
